[plugin]

name="dot-match"
description="The match loop: warmup, rounds, scoring, teams, spawning and respawning. Everything counted in ticks and driven by one call, so a server's round clock and its netcode agree about what time it is."
author="dot"
version="0.1.0"
script="dot_match_plugin.gd"
